config:

  # Copy the T3 Gemstone O1 board DTS and its pinmux include into the TI
  # device-tree source directory after normal kernel patches have been applied.
  dts-directories:
    - { source: "dt", target: "arch/arm64/boot/dts/ti" }
